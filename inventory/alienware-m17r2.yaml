host:
  id: alienware-m17r2
  os: arch
  gpu: nvidia
  cpu: intel
  ram_gb: 32
  storage_root: /srv
  model_dir: /srv/models
  ai_engine_model: DeepSeek-R1-Distill-Qwen-7B-Q4_K_M.gguf

projects:
  - ai-infra
  - ai-dev

platforms:
  - llama
  - openwebui

network:
  expose_ui: false